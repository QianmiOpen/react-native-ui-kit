const Config = {
  /**
   * 默认导航条样式
   */
  scene: {
    defaultHeaderStyle: null,
  },

  /**
   *
   */
  fetch: {
    host: '',
    success: (r) => {
    },
    fail: (r) => {
    },
  }
}

export default Config;
