const Config = {
  /**
   * 默认导航条样式
   */
  scene: {
    defaultHeaderStyle: null,
  },

  /**
   *
   */
  fetch: {
    host: '',
    handler: (responseText) => {
    }
  }
}

export default Config;
